#Exercice sur les bases de l'HTML

L'HTML est la pierre angulaire du web.

C'est un langage sémantique, visant à structurer le contenu.

Dans la branche *exo*, vous avez les fichiers de base à modifier.

Dans la branche *fini*, vous avez la version corrigée.